import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.edu.piloto.macarena',
  appName: 'Macarena en Movimiento',
  webDir: 'out'
};

export default config;
